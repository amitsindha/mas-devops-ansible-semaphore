apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-persistent-storage
  labels:
    app: wordpress
  namespace: semaphore
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 6Gi
